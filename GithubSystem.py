#!/usr/bin/python3
import os, io, sys, platform, shutil, urllib3, subprocess

class GithubTools():
    
  def read_metadata(item_name)
      read item_name from meta/data.txt;
      return item_value;

  def modify_metadata( item_name, item_value)
      modify item_name from meta/data.txt using item_value;
       
  def output_log()
   {
   history | grep mgithub
   cat /log/mgithub.log
   }
   
  def get_list(){
    get lists from URL
    return lists;
   }
   
  def git_clone(repository_name){
    get clone from URL/repository_name
   }


   def push_repo (comment)
   {
   try:
      git add
      git commit "comment"
      git push

   catch exception:
      git push
   }

   return status;
   }
   
   def print_log (repository,command,status){
    }


  def delete_line (key, file){

  }
